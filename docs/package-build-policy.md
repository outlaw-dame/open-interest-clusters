# Package build policy

Source files are authoritative. The `dist/` directory is generated by `npm run build`, ignored by Git, and should not be edited or maintained by hand.

Before publishing or validating package entrypoints, run:

```bash
npm run clean
npm run build
npm test
```

Generated package files must come from the TypeScript compiler rather than manual edits.
